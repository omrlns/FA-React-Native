// while -> enquanto 
var x = 0

while (x < 10) {
    document.write('<br> o valor de x é: ' + x)
    // x = x + 1
    x++
}

// for = para
var limite = 29

for (a = 0; a <= limite; a++) {
    document.write('<br> o valor de a é: ' + a)
}